package WebCrawler.WebCrawler;

/**
 * This class will traverse the tree finding the nodes where books, movies or songs are stored
 * When something is found then an API will be called in order to create the api for that book
 */
public class GoInTree {
    public static void goInTree(Tree t)
    {
        for (Node aux: t.listNodes) {
            
        }
    }
}
